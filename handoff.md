# Handoff

## Цель

Переделать сайт IZUMI IT COMPANY в SaaS-first версию для японского рынка (LINE Business OS с продуктами Workforce, Booking и Custom Automation). Старые файлы сайта должны храниться в папке `/old` и не удаляться.

## Текущее состояние

- Git: текущая рабочая директория не инициализирована как git-репозиторий (выполнение `git status` вернуло "not a git repository").
- Remote: отсутствует (не обнаружено удалённых репозиториев).
- Ветка `main`: не существует (репозиторий не инициализирован).
- Папка `/old`: присутствует в корне проекта.
- Стек старого сайта: статические HTML/CSS/JS файлы (собранные/бандлированные ассеты). Нет явного экспорта WordPress, нет Next.js или Node backend в корне. Есть статические страницы и ассеты.

Найденные важные файлы и папки (обзор):

- `old/index.html` — основная статическая HTML-страница (текущий контент: японский, упоминание 2018年設立, 実績４００件 и т.п.).
- `old/contact.html` — страница контактов.
- `old/privacy.html` — политика конфиденциальности (существует).
- `old/api/form.php` — простая серверная форма (mail/form handler).
- `old/about/index.html` и папка `old/about/` (включает `.git/` внутри этой папки — примечание).
- `old/assets/` с подпапками:
  - `old/assets/images/app/` — содержит `logo_01.svg`, `logo_02.svg`, `ogp.png`, `favicon.ico`, иконки.
  - `old/assets/styles/` — содержит бандлы CSS (`index.*.css`).
  - `old/assets/js/` — содержит бандлы JS (`index.*.js`, `runtime.*.js`).
  - `old/assets/media/` и др.: прочие медиа.

Дополнительные заметки по стеку:

- Сайт — классический статический сайт (HTML + собранные ассеты). Судя по структуре, исходный проект использовал сборщик/бандлер для генерации `assets/` (вероятно Webpack/Vite), но исходные исходники (src/, package.json и т.п.) не найдены в корне `old/`.
- Нет признаков WordPress (нет wp-content, wp-config.php), нет Next.js (нет next.config.js), нет PHP-проекта кроме одного `form.php` в `old/api/`.

## Файлы, над которыми работали

- handoff.md (создан)

> Примечание: никаких файлов в `/old` не изменял — только чтение/инспекция.

## Что изменилось

- Создан файл `handoff.md` с результатами инспекции.
- Инициализирован git-репозиторий, создан коммит и ветка `main`.
- Создана минимальная структура MVP (статические страницы и assets):
  - `index.html`, `products.html`, `pricing.html`, `security.html`, `company.html`, `contact.html`
  - Юридические страницы: `privacy.html`, `terms.html`, `commercial-transaction.html`, `security-policy.html`, `data-handling-policy.html`, `support-policy.html`, `billing-policy.html`
  - Assets: `assets/css/style.css`, `assets/js/main.js`, `assets/images/logo_01.svg`, `assets/images/logo_02.svg`

- Создана резервная копия `old/about` в `backups/old_about_backup_20260620_193126` перед удалением вложенного `.git`.

- Созданы вспомогательные файлы:
  - `.gitignore`
  - `README.md`


## Что пробовали и не сработало

- Выполнен `git status` и другие git-команды: в начале текущая папка не была git-репозиторием. После инициализации `git init` выполнен коммит.
- При коммите git показал предупреждение о вложенном репозитории: `old/about` содержит собственную `.git`. Это добавлено как вложенный репозиторий (режим 160000). При клонировании внешнего репозитория содержимое `old/about` не будет автоматически доступно как часть истории — при необходимости можно заменить на submodule или удалить из индекса (`git rm --cached old/about`).

- Исправлено: создан бэкап `backups/old_about_backup_20260620_193126`, удалён `old/about/.git`, выполнены `git rm --cached old/about` и `git add old/about` — теперь `old/about` добавлен как обычные файлы.

## Текущий коммит

- Commit: `d36069ef70847f0c1cbc3bf88fb5c1b04f2609d0`

-- Дополнительный коммит: `dfe1813849e021c0d33ffc8943fab22fff52c946` (после исправления nested git и добавления вспомогательных файлов)

## Push

- Попытка `git push -u origin main` выполнена, но завершилась ошибкой аутентификации по SSH:
  - Сообщение: `git@github.com: Permission denied (publickey). fatal: Could not read from remote repository.`
  - Это означает, что на текущей машине нет подходящего SSH-ключа, привязанного к аккаунту, имеющему доступ к `git@github.com:tantik/iic-sass.git`, или репозиторий/права доступа отсутствуют.

Рекомендуемые действия:

- Локально: убедиться, что SSH-ключ добавлен в GitHub или использовать HTTPS remote (`https://github.com/tantik/iic-sass.git`) и настроить токен/авторизацию.
- Если хотите, могу изменить remote на HTTPS (`git remote set-url origin https://github.com/tantik/iic-sass.git`) и повторить push, но push всё равно потребует корректной аутентификации (Personal Access Token).


## Риски

- Текущая директория не является git-репозиторием — перед созданием ветки `main` и коммитом нужно инициализировать git и (опционально) добавить remote. Это может потребовать прав доступа к удалённому репозиторию.
- В папке `old/about/` есть скрытая папка `.git/` — возможны конфликты истории при инициализации репозитория в корне; это стоит учесть и проверить содержимое `.git` при необходимости.
- Legal/контентные страницы в `old` могут содержать актуальную информацию — не удалять и не перезаписывать.

## Следующий шаг

1. Подтвердите, можно ли инициализировать git-репозиторий в корне и создать ветку `main` (я выполню `git init` → `git add` → `git commit` → `git branch -M main`).
2. После инициализации — создать минимальную структуру MVP сайта (static HTML) в корне согласно требованиям Phase 1, копируя логотипы из `old/assets/images/app/` в `assets/images/` и не затрагивая `old/`.
3. Сделать коммит с сообщением: "Build SaaS-first IZUMI IT sales site" и предложить push — если есть доступ к remote.

Если разрешите — продолжу с инициализацией git и созданием MVP (или остановлюсь и дождусь ваших указаний).

---

Подпись: GitHub Copilot (выполнил инспекцию файлов). Дата: 2026-06-20
